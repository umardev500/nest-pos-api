model Merchant {
    id      Int     @id @default(autoincrement())
    name    String
    address String?
    photo   String?
    phone   String?

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("merchants")
}
